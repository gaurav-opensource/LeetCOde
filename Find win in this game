class Solution {
    public boolean canAliceWin(int[] nums) {
        int length = nums.length;
        int AliceSum = 0;
        int  BobSum = 0;
        for(int i=0;i<length;i++){
            if(nums[i]<10){
                BobSum += nums[i];
            }else{
                 AliceSum += nums[i];

            }
             }
            if( BobSum ==  AliceSum){
                return false;
            }
                return true;
            
        }
        
       
}